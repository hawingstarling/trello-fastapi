version: "3.4"

services:
  postgresql:
    image: postgres:14.2-alpine
    environment:
      POSTGRES_URL: postgres
      POSTGRES_PASSWORD: hungdat!234
      POSTGRES_DB: trello_fastapi
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    